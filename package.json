{
    "name": "roadmap2026-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "backend": "cd backend && gradlew.bat bootRun",
        "frontend": "cd frontend && npm run dev",
        "dev": "concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}